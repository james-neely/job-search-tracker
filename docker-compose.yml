services:
  app:
    build:
      context: .
      target: dev
    ports:
      - "3001:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
      - ./data:/app/data
    environment:
      - NODE_ENV=development
      - HOSTNAME=0.0.0.0

volumes:
  node_modules:
