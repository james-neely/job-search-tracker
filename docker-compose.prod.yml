services:
  app:
    build:
      context: .
      target: production
    ports:
      - "3001:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
